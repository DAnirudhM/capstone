import { RemoveWhiteSpacePipe } from './remove-white-space-pipe.class';

describe('RemoveWhiteSpacePipe', () => {
  it('should create an instance', () => {
    expect(new RemoveWhiteSpacePipe()).toBeTruthy();
  });
});
